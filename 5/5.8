class Human
{
    private int size;
    private String name;

    public String predstav()
    {
        String str;
        if (size<150)
            str = name+" маленький человек";
        else if (size>200)
            str = name+" большой человек";
        else
            str = name+" средний человек";
        return str;
    }

    public Human (int size,String name)
    {
        this.size = size;
        this.name = name;
    }
}
